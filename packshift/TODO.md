# PackShift – Implementation TODO

- [x] Update `index.html` marquee to seamless loop (duplicate track).

- [ ] Tweak hero/services markup if needed for better reveal + interactive states.
- [x] Update `style.css` to add/adjust: stronger service card hover lift/shadow, shake-on-error styles, and cursor blending/scale polish.
- [x] Update `script.js` contact form UX: shake invalid fields + button state timeline like `SHIFTING DATA...` → `SUCCESS!`.

- [x] Verify reduced-motion behavior still stops marquee/truck/grain.

- [x] Run quick local test (static server) and confirm interactions.


